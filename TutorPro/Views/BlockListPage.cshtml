@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.BlockListPage>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@{
    var rootPage = (HomePage)Model.Root();
    if (rootPage.TContentPage != null && rootPage.TContentPage.Id == Model.Id)
    {
        Layout = null;
    }
    else
    {
        Layout = "Main.cshtml";
    }
    ViewBag.CurrentPage = Model;
    ViewBag.ParentPage = Model.Parent;
}

@await Html.GetBlockListHtmlAsync(Model.TBlockList)